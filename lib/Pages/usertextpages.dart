import 'package:flutter/material.dart';

class UserTextPage
 extends StatelessWidget {
  const UserTextPage
  ({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('mis textos'),
    );
  }
}